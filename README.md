# CityInsights_TelegramBot
Telegram bot that forwards raw user messages to a Langflow flow and returns a clean, human-readable reply. Users can set area (typed or via Telegram location) and date range (days). Includes slow-flow timeouts, JSON parsing to extract message text, and a simple onboarding UX.
